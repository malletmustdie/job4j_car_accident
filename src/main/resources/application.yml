spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5433/auto_crash
    username: postgres
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    properties.hibernate:
      show_sql: true
      format_sql: true
      hbm2ddl.auto: validate
server:
  port: 8082